/*const axios = require("axios");
// Configure the path to read the environment variable file, .env, to get api key
require('dotenv').config({path: "./fill_this_in_later"})
const baseUrl = "Put_the_url_path_or_key_here";
// Sea class for openSea
class Sea{
    // Introduce the parameters needed
    // get dat with async 
    // url variable for certain data
    let url = ...
    return (await axios(url)).data;
}
*/